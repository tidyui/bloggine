<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400&family=Bitter:wght@700&display=swap">
    <link rel="stylesheet" href="~/theme/css/style.min.css" asp-append-version="true">

    <title>@ViewData["Title"]</title>

    @RenderSection("head", required: false)
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
            <a class="navbar-brand" href="~/">@(Blog.Settings.Title).</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    @foreach (var post in Blog.GetPosts(p => p.Category == "Docs" && p.Settings.IsPinned))
                    {
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" href="~/@post.Slug">@post.Title</a>
                        </li>
                    }
                </ul>
            </div>
        </div>
    </nav>
    <main>
        @RenderBody()
    </main>
</body>
</html>